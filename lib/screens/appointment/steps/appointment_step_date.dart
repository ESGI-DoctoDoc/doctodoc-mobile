import 'package:flutter/material.dart';

class AppointmentStepDate extends StatelessWidget {
  const AppointmentStepDate({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "AppointmentStepDate",
      ),
    );
  }
}
