import 'package:flutter/material.dart';

class AppointmentStepMedicalConcern extends StatelessWidget {
  const AppointmentStepMedicalConcern({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "AppointmentStepMedicalConcern",
      ),
    );
  }
}
