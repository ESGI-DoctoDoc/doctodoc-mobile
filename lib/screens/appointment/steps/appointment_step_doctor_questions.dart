import 'package:flutter/material.dart';

class AppointmentStepDoctorQuestions extends StatelessWidget {
  const AppointmentStepDoctorQuestions({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "AppointmentStepDoctorQuestions",
      ),
    );
  }
}
