import 'package:flutter/material.dart';

class AppointmentStepCareTracking extends StatelessWidget {
  const AppointmentStepCareTracking({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "AppointmentStepCareTracking",
      ),
    );
  }
}
